services:
  - type: web
    name: spa-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install && npm run build
    startCommand: cd backend && node dist/scripts/db-init.js && node dist/index.js
    branch: main
    envVars:
      - key: DATABASE_URL
        sync: false # Set this manually in Render Dashboard to your paid DB link
      - key: REDIS_URL
        value: rediss://default:AUrLAAIncDJlYjkyNGE2YTFmOWY0OWQ5YjZjYWE2MWU0ZTA2NzAwN3AyMTkxNDc@helpful-basilisk-19147.upstash.io:6379
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22

  - type: web
    name: spa-frontend
    env: node
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd frontend && npm start
    branch: main
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://spa-backend-mvb1.onrender.com
      - key: NODE_VERSION
        value: 22


# Redis service (Render requires paid plan for Redis usually, or external provider)
# This is a placeholder as Render YAML support for Redis varies by plan level
# For free tier, might need external Redis or internal docker service
